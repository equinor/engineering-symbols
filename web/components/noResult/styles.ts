import styled from 'styled-components';

export const NoResultStyled = styled.div`
	max-width: 20rem;
	margin: 4rem auto;

	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);

	svg {
		width: 20rem;
	}

	p {
		text-align: center;
		padding: 1rem 0;
		font-size: 18px;
	}
`;
